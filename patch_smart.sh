sed -i '216,222c\            } catch (jsonErr) {\n                try {\n                    const sanitized = cleanText.replace(/[\\x00-\\x1F]/g, " ");\n                    const data = JSON.parse(sanitized);\n                    matchResults = data.matches || [];\n                    usedAI = true;\n                } catch (fallbackErr) {\n                    console.warn("AI Smart Match JSON invalid:", text);\n                    usedAI = false;\n                }\n            }' services/aiService.ts
